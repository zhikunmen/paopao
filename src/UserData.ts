class UserData {
    public static nick: string = "";
    public static userInfo: gmbox.IUserInfo = null;

    public static init(): Promise<any> {
        return new Promise((resolve) => {
        })
    }
}